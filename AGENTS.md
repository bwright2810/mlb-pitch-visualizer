# Repository Instructions

- Deploy applications through the Coolify API using Sprite’s authenticated API gateway. Do not deploy them as Sprite services or use raw API credentials.
- When a functional change is complete, commit the changes to git, push the current branch to its configured remote, and deploy the updated application through the Coolify API gateway.
